/**
 * Fundamentals: reference implementations of the core data structures and
 * algorithms, written to be read.
 *
 * <p>Every class in this tree follows the same three rules:</p>
 * <ol>
 *   <li>The data structure is built from scratch - no {@code java.util} shortcut
 *       for the thing being taught (a hash map implementation may not use
 *       {@link java.util.HashMap}).</li>
 *   <li>Every public method states its time and space complexity in Javadoc.</li>
 *   <li>Where a problem has more than one classic solution (iterative vs
 *       recursive, naive vs optimized), both are kept side by side so the
 *       trade-off is visible.</li>
 * </ol>
 *
 * <p>Reading order for a beginner:
 * {@code complexity} &rarr; {@code arrays} &rarr; {@code strings} &rarr;
 * {@code linkedlist} &rarr; {@code stack} &rarr; {@code queue} &rarr;
 * {@code hashing} &rarr; {@code tree} &rarr; {@code heap} &rarr;
 * {@code graph} &rarr; {@code searching} &rarr; {@code sorting} &rarr;
 * {@code recursion} &rarr; {@code dp}.</p>
 *
 * <p>Long-form theory for each package lives in {@code docs/}.</p>
 */
package com.github.codemaster.fundamentals;
